<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    
    package="happyface.rpgtraveler.com"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-sdk
        android:minSdkVersion="5"
        android:targetSdkVersion="18" />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppBaseTheme">
        <activity
            android:name="happyface.rpgtraveler.com.Act00LogonMaster"
            android:label="@string/app_name" 
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:name=".Act10GameManager"/>
        <activity
            android:label="@string/app_name"
            android:name = ".Act11GroupManager"/>
        <activity
            android:label="@string/app_name"
            android:hardwareAccelerated="false"
            android:name = ".Act12CharacterManager"/>
        <activity
            android:label="@string/app_name"
            android:hardwareAccelerated="false"
            android:name = ".Act20MainGamePlay"/>
    </application>

</manifest>
